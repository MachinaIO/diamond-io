# dio

diamond-io cli implementation

### run 
```
cargo run -- run -c dio-config.example.toml -v verify_circuit.example.json
```

### save circuit to file
```
cargo run -- gen-bench-circuit -o verify_circuit.example.json -c AND -n 2
```